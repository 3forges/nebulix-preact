---
import { t } from "@util/translate";
import { Icon } from "astro-icon";
const { name } = Astro.props;
---

<!--
  <button class="text-sm leading-none tracking-wider" aria-label={t(name)}>

    <Popper
    content={t(name)}
    client:visible
    hover
    placement="top"
    offsetDistance="1"
  >
    <span class="block transition-colors duration-500 hover:text-primary">
      <Icon name={name} class="pointer-events-none h-8 w-10 px-1" />
    </span>
  </Popper>
</button>
 -->
<button aria-label={t(name)} data-tooltip-target="tooltip-click" data-tooltip-trigger="click" type="button" class="leading-none tracking-wider  ms-3 text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">Tooltip click</button>

<div id="tooltip-click" role="tooltip" class="absolute z-10 invisible inline-block px-3 py-2 text-sm font-medium text-white bg-gray-900 rounded-lg shadow-sm opacity-0 tooltip dark:bg-gray-700">
    <span class="block transition-colors duration-500 hover:text-primary">
      <Icon name={name} class="pointer-events-none h-8 w-10 px-1" />
    </span>
    <div class="tooltip-arrow" data-popper-arrow></div>
</div>